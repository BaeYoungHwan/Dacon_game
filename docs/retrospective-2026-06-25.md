# 회고 — k-stock-merchant (떡상러쉬)

> 종료일: 2026-06-25 | 대회 최종 결과: 336명 중 19위 (떡상연구소)

---

## 1. 가장 잘 된 점

- **배영환·송원호 영역 분담 + Claude Code 협업 흥률**
  - `src/store/`, `src/lib/` (배영환) ↔ `src/pages/`, `src/components/` (송원호) 영역을 CLAUDE.md에 명시해 양쪽 모두 Claude Code를 쓰면서도 충돌 없이 병행 작업
  - 실제 데이터 기반 차별화(pykrx 주봉 수집 + 실제 금융 뉴스 207개)와 QA 자동화(Vitest 65+ 케이스, 5회 이상 완주 플레이테스트)도 안정성에 기여

## 2. 다음 프로젝트에서 개선하고 싶은 점

- **AI-Readiness 점수 개선** — 종료 시점 측정 13/100 (AI-Hostile). `src/`, `scripts/` 모듈에 별도 CLAUDE.md가 없어 탐색 비용이 컸고, god file(`MarketPage.jsx` 1490줄 등)도 분할되지 않음. 다음 프로젝트는 모듈별 컨텍스트 문서를 초기부터 병행 작성
- **1차 투표(대중 리더보드) 확보 실패 원인 복기** — top10 진출 기준에 못 미친 만큼, 대중·참가팀 투표 비중이 큰 평가 구조(60%+20%+20%)에서 초기 홍보·노출 전략이 부족했는지 짚어볼 필요

## 3. Claude Code 활용에서 효과적이었던 패턴

- **WBS-v2.md 단일 진실 공급원 + 진행도 추적** — 컨텍스트 재시작 시 이 파일 하나만 읽어도 전체 진행률(150/150)과 다음 작업을 즉시 파악 가능
- **팀 영역 제한을 CLAUDE.md에 명시한 권한 경계** — "수정 가능 영역" 표로 두 사람의 Claude Code 사용 범위를 코드가 아닌 문서로 박아둬서, 서로 다른 세션에서도 영역 침범 없이 작업

---

*이 문서는 `/close-project` 종료 절차의 일부로 생성됨.*
